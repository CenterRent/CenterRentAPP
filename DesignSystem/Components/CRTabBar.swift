import SwiftUI

// MARK: - Tab Bar
// The main app tab bar uses the native iOS 26 Tab API in MainTabView.swift.
// Apple's Tab(role: .search) automatically creates the separate search pill
// with Liquid Glass material applied by the OS — no custom implementation needed.
//
// CRTabItem / CRTabBar (for in-page segment tabs) live in CRNavigationBar.swift.
